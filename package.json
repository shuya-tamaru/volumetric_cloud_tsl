{
  "name": "bubble-tsl",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "lil-gui": "^0.20.0",
    "three": "^0.180.0"
  }
}
